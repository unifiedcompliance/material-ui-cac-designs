import {
    Container as MuiContainer,
} from "@material-ui/core";
import styled from "styled-components/macro";

const StepContainer = styled(MuiContainer)`
  height: 450px;
`

export default StepContainer